// from celsius to fahrenheit
let celsiusTemperature = 10;
let toFahrenheit = celsiusTemperature * 1.8 + 32;

// from fahrenheit to celsius
let fahrenheitTemperature = 15;
let toCelsius = (fahrenheitTemperature - 32) * (5 / 9);

// exercises (calculate customer total payment after off percent)
let totalPayment = 250_000;
let offPercent = 25;

totalPayment -= totalPayment * (totalPayment * 0.25);
